# Provenance — lib-q-lattice-zkp/tests/vectors/manifest.json

Self-generated: the file's own `schema` field is `"lattice-zkp-kat-v0"` and it names its own
regeneration command (`cargo test -p lib-q-lattice-zkp kat_regenerate_vectors -- --ignored`).
Fixed-seed prove/verify vectors for lib-q-lattice-zkp wire v0, produced by this crate's own code
under test.

## Machine-checked sidecar lines

- `manifest.json`: origin=self-generated; fixed-seed prove/verify vectors for lib-q-lattice-zkp wire v0, regenerated by its own kat_regenerate_vectors test
