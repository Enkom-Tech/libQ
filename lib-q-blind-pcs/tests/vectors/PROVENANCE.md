# Provenance — lib-q-blind-pcs/tests/vectors/manifest.json

Self-generated: the file's own `schema` field is `"blind-pcs-demo-v1"` and it names its own
regeneration command (`cargo test -p lib-q-blind-pcs demo_vectors -- --nocapture`). Demo-only
vectors for EXPERIMENTAL_NON_NIST blind PCS APIs, produced by this crate's own code under test.

**These are not a conformance oracle, and do not treat them as one.** Being this crate's own
output, they move with the implementation and cannot detect a change to the commitment format —
which is not hypothetical: `df33c57` swapped the commitment hash SHA-256 -> SHA3-256, changing
every commitment byte, and nothing in the tree went red (board card `ENK-244`).

The detector for that is `../commitment_kat.rs`, whose expected digests come from **Python's
`hashlib.sha3_256`** rather than from this crate, and which is therefore the file to consult (and
to recompute, with the script in its header) if the commitment format ever changes again.

## Machine-checked sidecar lines

- `manifest.json`: origin=self-generated; demo-only fixed-seed vectors for lib-q-blind-pcs's EXPERIMENTAL_NON_NIST blind PCS APIs, regenerated by its own `demo_vectors` test
