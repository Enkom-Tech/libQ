# Provenance — lib-q-mac/tests/vectors/*.json

Both files are self-generated: `manifest.json` names its own regeneration command
(`cargo test -p lib-q-mac kat_regenerate_vectors -- --ignored`) and `qcw-mac-v1.json` carries its
own schema tag `"format": "qcw-mac-kat-v1"`. Produced by lib-q-mac's own code under test, fixed-seed
(fixed `key_hex` in the manifest), not sourced from any external corpus.

## Machine-checked sidecar lines

- `manifest.json`: origin=self-generated; index/key metadata for the fixed-seed qcw-mac-kat-v1 vectors, regenerated by lib-q-mac's own kat_regenerate_vectors test
- `qcw-mac-v1.json`: origin=self-generated; fixed-seed positive/negative MAC verification vectors produced by lib-q-mac's own code under test
