# Provenance — lib-q-zkp/tests/vectors/recovery-policy-v{0,1}/manifest.json

Both files are self-generated: each names its own regeneration command
(`cargo test -p lib-q-zkp kat_regenerate_recovery_policy_vectors -- --ignored --release` for v0,
`kat_regenerate_recovery_policy_hybrid_vectors` for v1) and carries its own schema tag
(`"recovery-zkp-kat-v0"` / `"recovery-zkp-kat-v1"`). Fixed-seed recovery-policy STARK vectors
produced by lib-q-zkp's own code under test.

## Machine-checked sidecar lines

- `recovery-policy-v0/manifest.json`: origin=self-generated; fixed-seed recovery-policy STARK vectors, regenerated by lib-q-zkp's own kat_regenerate_recovery_policy_vectors test
- `recovery-policy-v1/manifest.json`: origin=self-generated; fixed-seed recovery-policy hybrid STARK vectors, regenerated by lib-q-zkp's own kat_regenerate_recovery_policy_hybrid_vectors test
