//! zk-STARK implementation
//!
//! TODO: Implement actual zk-STARK functionality

/// zk-STARK prover
pub struct StarkProver;

impl StarkProver {
    /// Create a new zk-STARK prover
    pub fn new() -> Self {
        Self
    }
}

impl Default for StarkProver {
    fn default() -> Self {
        Self::new()
    }
}

/// zk-STARK verifier
pub struct StarkVerifier;

impl StarkVerifier {
    /// Create a new zk-STARK verifier
    pub fn new() -> Self {
        Self
    }
}

impl Default for StarkVerifier {
    fn default() -> Self {
        Self::new()
    }
}
