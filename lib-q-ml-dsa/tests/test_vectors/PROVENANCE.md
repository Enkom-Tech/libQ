# Provenance — lib-q-ml-dsa/tests/test_vectors/interop_vectors.json

Self-generated by this crate's own `tests/wire_format_tests.rs` (`fs::write("tests/test_vectors/
interop_vectors.json", json)`), not sourced from any external corpus. It pins a fixed-seed
sign/verify interop check against a saved wire-format encoding of lib-q-ml-dsa's own output.

## Machine-checked sidecar lines

- `interop_vectors.json`: origin=self-generated; written by lib-q-ml-dsa tests/wire_format_tests.rs to pin a fixed-seed interop wire-format encoding of the crate's own sign/verify output
