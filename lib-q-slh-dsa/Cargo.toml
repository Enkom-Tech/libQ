[package]
name = "lib-q-slh-dsa"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
description = "SLH-DSA (SPHINCS+) implementation for lib-Q"
keywords = ["cryptography", "post-quantum", "signatures", "slh-dsa", "sphincs"]
categories = ["cryptography"]
exclude = ["tests"]

[dependencies]
hybrid-array = { version = "0.4.10", features = ["extra-sizes"] }
typenum = { version = "1.19.0", features = ["const-generics"] }
lib-q-core = { path = "../lib-q-core", version = "0.0.2", features = ["alloc"] }
lib-q-sha3 = { path = "../lib-q-sha3", version = "0.0.2", default-features = false }
lib-q-random = { path = "../lib-q-random", version = "0.0.2", optional = true, features = ["signature"] }
zerocopy = { version = "0.8.48", features = ["derive"] }
rand_core = { workspace = true }
signature = { version = "3.0.0-rc.10", features = ["rand_core"] }
hmac = "0.13.0"
sha2 = { version = "0.11.0", default-features = false }
digest = { workspace = true }
pkcs8 = { version = "0.11.0-rc.11", default-features = false, features = ["alloc"] }
const-oid = { version = "0.10.2", features = ["db"] }
zeroize = { workspace = true, optional = true, default-features = false }
getrandom = { workspace = true, optional = true, default-features = false }

[dev-dependencies]
hex-literal = "1.1.0"
hex = { workspace = true, features = ["serde"] }
lib-q-random = { path = "../lib-q-random", version = "0.0.2", features = ["std", "signature"] }
num-bigint = { workspace = true }
quickcheck = "1.1.0"
quickcheck_macros = "1.2.0"
proptest = { workspace = true }
criterion = { workspace = true }
aes = { workspace = true }
cipher = "0.5.1"
ctr = "0.10.0"
pastey = { workspace = true }
serde_json = { workspace = true }
serde = { workspace = true, features = ["derive"] }
pkcs8 = { version = "0.11.0-rc.11", features = ["pem"] }

[lib]
bench = false

[[bench]]
name = "sign_verify"
harness = false

[[bench]]
name = "lib_q_benchmarks"
harness = false

[features]
default = ["alloc", "pkcs8/alloc", "std"]
alloc = []
std = ["alloc", "dep:lib-q-random", "lib-q-random/std"]
no_std = ["getrandom", "alloc", "dep:lib-q-random", "lib-q-random/no_std", "lib-q-random/getrandom"]
test = []
zeroize = ["dep:zeroize", "hybrid-array/zeroize"]
# Expand `test_parameter_sets!`, ACVP keygen projection, and integration KATs to all 12 FIPS parameter sets and slow `*s` vectors (slow). Default smoke uses Shake128f + Sha2_128f only and skips `*s` ACVP groups.
all-parameter-set-tests = []
