[package]
name = "lib-q-threshold-sig"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
homepage.workspace = true
repository.workspace = true
readme = "README.md"
# WITHDRAWN: this crate is not a signature scheme and provides no security. Every signing and
# verification entry point fails closed; the construction behind them has been deleted. See
# README.md and the crate-level documentation.
description = "WITHDRAWN — cryptographically unsound, provides no security; all entry points fail closed"
keywords = ["cryptography", "withdrawn", "deprecated", "insecure", "threshold"]
categories = ["cryptography", "wasm"]
# Belt-and-suspenders alongside the deleted construction: `publish = false` means `cargo publish`
# refuses this crate outright, even a manual `cargo publish -p lib-q-threshold-sig` run by a
# maintainer. It is intentionally absent from cd.yml's crates.io and npm publish jobs; this makes
# that absence load-bearing instead of merely incidental.
publish = false


[package.metadata.cargo-machete]
ignored = [
    "getrandom",
    "serde",
]
[dependencies]
lib-q-core = { path = "../lib-q-core", version = "0.0.10", optional = true, default-features = false }
lib-q-random = { path = "../lib-q-random", version = "0.0.10", default-features = false, features = ["std"] }
zeroize = { workspace = true, default-features = false, features = ["alloc"] }
subtle = { workspace = true, default-features = false }
rand_core = { workspace = true }
wasm-bindgen = { workspace = true, optional = true }
getrandom = { workspace = true, optional = true, default-features = false }

[target.'cfg(all(target_arch = "wasm32", target_os = "unknown"))'.dependencies]
getrandom = { workspace = true, default-features = false, features = ["wasm_js"] }

[dev-dependencies]
hex = { workspace = true }
serde = { workspace = true, features = ["derive", "std"] }
serde_json = { workspace = true, features = ["std"] }
wasm-bindgen-test = { workspace = true }

[features]
default = ["std", "random"]
std = []
random = []
wasm = [
    "wasm-bindgen",
    "std",
    "random",
    "dep:lib-q-core",
    "lib-q-core/std",
    "lib-q-core/wasm",
    "lib-q-random/wasm",
    "dep:getrandom",
]
getrandom = ["dep:getrandom"]

[lib]
name = "lib_q_threshold_sig"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[[test]]
name = "wasm_smoke"
path = "tests/wasm_smoke.rs"
required-features = ["wasm", "random"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(feature, values("std", "random", "wasm", "getrandom"))',
] }
