# Provenance — lib-q-blind-token/tests/vectors/*.json

Both files are self-generated: `manifest.json` names its own regeneration command
(`cargo test -p lib-q-blind-token kat_regenerate_vectors -- --ignored`) and `blind-token-v2.json`
carries its own schema tag `"format": "blind-token-kat-v2"`. Produced by lib-q-blind-token's own
code under test (`tests/kat_regenerate.rs`), fixed-seed, not sourced from any external corpus.

## Machine-checked sidecar lines

- `manifest.json`: origin=self-generated; index/metadata for the fixed-seed blind-token-kat-v2 vectors, regenerated by lib-q-blind-token's own kat_regenerate_vectors test
- `blind-token-v2.json`: origin=self-generated; fixed-seed re-randomized ZK proof-of-possession vector produced by lib-q-blind-token's own code under test
